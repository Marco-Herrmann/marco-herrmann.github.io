# marco-herrmann.github.io

Personal homepage, served by GitHub Pages at https://marco-herrmann.github.io.

Plain HTML/CSS, no build step. Edit `index.html` (content) and `style.css` (styling), then push to `main`.
To add a profile photo, put it at `images/profile.jpg` and uncomment the `<img>` line in `index.html`.
